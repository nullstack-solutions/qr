# Python dependencies for CI visual regression checks.
# Keep versions pinned for reproducible installs in the GitHub Actions environment.
numpy==2.1.3
opencv-python-headless==4.10.0.84
pillow==10.4.0
zxing-cpp==2.3.0
qrcode==7.4.2
